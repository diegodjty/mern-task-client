import{
    SIGNIN_SUCC,
    SIGNIN_ERROR,
    GET_USER,
    LOGIN_SUCC,
    LOGIN_ERROR,
    LOGOUT
} from '../../types'

export default( state , action) =>{
    switch(action.type){
        
        default:
            return state;
    }
}